services:
  web:
    build:
      context: .
      target: web
    env_file: .env
    restart: unless-stopped
    expose:
      - "3000"

  cron-worker:
    build:
      context: .
      target: worker
    env_file: .env
    restart: unless-stopped
    depends_on:
      - web

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # nginx.active.conf wird vom deploy.sh ausgewählt:
      # - beim ersten Boot: nginx-bootstrap.conf (HTTP only, für ACME)
      # - sobald Zertifikat liegt: nginx.conf (HTTP+HTTPS)
      - ./nginx/nginx.active.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/certbot/conf:/etc/letsencrypt
      - ./nginx/certbot/www:/var/www/certbot
    depends_on:
      - web

  certbot:
    image: certbot/certbot
    restart: unless-stopped
    volumes:
      - ./nginx/certbot/conf:/etc/letsencrypt
      - ./nginx/certbot/www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew --quiet; sleep 12h & wait $${!}; done;'"
